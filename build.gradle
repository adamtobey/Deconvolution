apply plugin: 'java'

dependencies {
	compile "java.javax:javax:1"
	compile "org.emjl:emjl:0.7"
}

jar {
    manifest {
        attributes(
			'Main-Class': 'com.adamtobey.deconvolution.Runner',
			'Class-Path': configurations.runtime.files.collect { it.getName() }.join(' ')
		)
    }
}
